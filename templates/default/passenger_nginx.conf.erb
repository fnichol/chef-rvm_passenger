passenger_root <%= node[:rvm_passenger][:root_path] %>;
passenger_ruby <%= node[:rvm_passenger][:ruby_wrapper] %>;

<% if not @directives.empty? -%>
<% @directives.each do |d,v| -%>
<%= d -%> <%= v %>;
<% end %>
<% end %>

